{
  "name": "sol-hud",
  "version": "0.4.1",
  "main": "main.js",
  "scripts": {
    "check": "node --check main.js && node --check preload.js && node --check renderer.js && node --check storage/playerStore.js",
    "test": "npm run check",
    "start": "electron ."
  },
  "dependencies": {
    "electron": "^28.0.0"
  }
}
